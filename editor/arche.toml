# arche-editor — the `editor` device: dom (<textarea>) + clib (vendored terminal editor) backends.
[lib]
paths = ["../ui/devices", "../../arche/extras", "../../arche/stdlib"]

[select]
editor = "dom"
