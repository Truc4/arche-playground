# arche-playground composes the components (../arche-editor, ../arche-terminal) into an in-browser GUI:
# write Arche, compile it client-side (the compiler is wasm), run it. It ships no devices of its own.
[lib]
paths = ["../arche/stdlib"]
